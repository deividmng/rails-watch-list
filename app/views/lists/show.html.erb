<h1> ver pelis</h1>

<h1><%=  @list.name %></h1>
<p>there are <%= @list.movies.count %>movies un this list</p>

<%= link_to "add movie", new_list_bookmark_path(@list), class: "btn btn-primary mt-2 mr-2" %>
