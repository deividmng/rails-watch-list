
<h1>Ver Películas</h1>

<h1>Probando para eliminar</h1>
<h1><%= @list.name %></h1>
<p>There are <%= @list.movies.count %> movies in this list</p>
<%= link_to "Add a Movie", new_list_bookmark_path(@list), class: "btn btn-primary mt-2 mr-2" %>

<ul class="list-group">
  <% @list.bookmarks.each do |bookmark| %>
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <%= bookmark.movie.title %> - <%= bookmark.comment %>
      <%= link_to "🗑", bookmark_path(bookmark), data: { turbo_method: :delete }, class: "btn btn-danger" %>
    </li>
  <% end %>
</ul>
