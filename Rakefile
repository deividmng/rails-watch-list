# Add your own movies in files placed in lib/movies ending in .rake,
# for example lib/movies/capistrano.rake, and they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_tasks
